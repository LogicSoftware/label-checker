---
name: Label Checker
on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - labeled
      - unlabeled

jobs:
  check_labels:
    name: Check labels
    runs-on: ubuntu-latest
    steps:
      - uses: LogicSoftware/label-checker@test1
        with:
          any_of: tested,partial
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/github-script@v3
        if: ${{ env.labels_check_passed == 'true' }}
        with:
          script: |
            // do nothing
